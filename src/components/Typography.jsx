export function SlideTitle({ children }) {
  return <h1 className="slide-title">{children}</h1>;
}

export function Highlight({ children }) {
  return <strong className="text-highlight">{children}</strong>;
}
